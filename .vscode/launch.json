{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/src/luajit",
            "args": [
		"-jdump",
                "${workspaceFolder}/tests/hello.lua"
            ],
            "cwd": "${workspaceFolder}/src"
        }
    ]
}